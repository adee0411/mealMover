import "./Footer.scss";
import Logo from "../UI/Logo";

const Footer = () => {
  return (
    <footer className="footer">
      <Logo variant="inverted" />
    </footer>
  );
};

export default Footer;
